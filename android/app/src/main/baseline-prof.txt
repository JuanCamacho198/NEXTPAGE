# Baseline Profile for NextPage — AOT-compiled startup paths
# These rules ensure critical user-facing screens are pre-compiled
# on first install, reducing cold-start JIT overhead.
#
# STATUS (SDD android-stack-modernization S11): STILL PLACEHOLDER CONTENT.
# The generation route now EXISTS — the androidx.baselineprofile consumer plugin is
# applied to :app and `:app:generateReleaseBaselineProfile` drives
# :benchmark's BaselineProfileGenerator (the four real journeys) — but collection
# needs a booted device/emulator, which this branch was authored without. This file
# has therefore NOT been replaced by a generated artifact yet. To produce the real
# profile, dispatch the `Android baseline profile (dispatch only)` CI job and commit
# the profile it uploads. Until then these hand-maintained class-only rules are the
# shipped profile.
#
# Generated manually (macrobenchmark:generateBaselineProfile pending).
# Format: <class descriptor>; (AGP class rule — method flags are not
# supported on class-only rules).
# NOTE: some descriptors below resolve to top-level function files rather than
# classes (e.g. BookGridSection/FilterSheetContent/LibraryDialogs/LibraryToolbar live
# in *Kt classes) and are dropped by profgen; real generation replaces the whole list.

# LibraryScreen — first screen most users see
Lcom/nextpage/presentation/screen/LibraryScreen;
Lcom/nextpage/presentation/screen/library/BookGridSection;
Lcom/nextpage/presentation/screen/library/FilterSheetContent;
Lcom/nextpage/presentation/screen/library/LibraryDialogs;
Lcom/nextpage/presentation/screen/library/LibraryToolbar;
Lcom/nextpage/presentation/viewmodel/LibraryViewModel;

# HomeScreen — dashboard after auth or cold start
Lcom/nextpage/presentation/screen/HomeScreen;
Lcom/nextpage/presentation/viewmodel/HomeViewModel;

# ReaderScreen — reading experience
Lcom/nextpage/presentation/screen/ReaderScreen;
Lcom/nextpage/presentation/viewmodel/ReaderViewModel;

# AuthScreen — login/register
Lcom/nextpage/presentation/screen/AuthScreen;
Lcom/nextpage/presentation/viewmodel/AuthViewModel;
