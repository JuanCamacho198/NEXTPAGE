package com.nextpage.presentation.viewmodel.library

/**
 * UI-level import state for the library.
 * Managed by [BookImportStateHolder].
 */
data class BookImportState(
    val isImporting: Boolean = false
)
