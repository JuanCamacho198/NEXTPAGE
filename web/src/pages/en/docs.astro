---
import BaseLayout from '../../layouts/BaseLayout.astro';
import DocsView from '../../components/DocsView.astro';
import { t } from '../../i18n';

const locale = 'en' as const;
---
<BaseLayout title={`${t(locale, 'docs.title')} — NextPage`}>
  <DocsView locale={locale} />
</BaseLayout>
