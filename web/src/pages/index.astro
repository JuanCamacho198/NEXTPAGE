---
import BaseLayout from '../layouts/BaseLayout.astro';
import HomeView from '../components/HomeView.astro';
import { t } from '../i18n';

const locale = 'es' as const;
---
<BaseLayout title={`${t(locale, 'home.title')} — NextPage`}>
  <HomeView locale={locale} />
</BaseLayout>
